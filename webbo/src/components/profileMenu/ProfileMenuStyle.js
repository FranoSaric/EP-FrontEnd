import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
    menu: {
        display: "flex",
        textDecoration: "none",
        color: "inherit",
    },
    color: {
        color: "#475569",
    },
}));

export default useStyles;
